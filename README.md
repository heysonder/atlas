# Atlas

A native, privacy-respecting YouTube client for iOS, built on [Piped](https://github.com/TeamPiped/Piped).
[SwiftUI](https://developer.apple.com/swiftui/) + Liquid Glass, targeting iOS 26.

## Features

- **Feed** — local subscriptions with an aggregated unauthenticated feed, plus a
  personalized "For You" mode and an option to hide Shorts.
- **Channels** — channel pages with subscribe / unsubscribe.
- **Search** — videos and channels, with query suggestions.
- **Player** — native AVPlayer (HLS with progressive fallback), Picture-in-Picture,
  AirPlay, background audio, SponsorBlock skipping, and resume-from-last-position.
- **Video info** — a slide-up sheet with the channel row, a `+` / `✓` subscribe
  toggle, a collapsible description, and comments with reply threads.
- **Collaborators** — videos with multiple creators show an overlapping avatar
  cluster; tapping it opens a picker to jump to any collaborator's channel.
- **Downloads** — offline video downloads.
- **Profile** — watch history and settings with a Piped instance picker.

## Architecture

- **`PipedKit/`** — a Swift package: Codable models, `PipedClient` (async API), instance directory,
  and pure stream-selection logic. No UI; unit-tested (`swift test`).
- **`Atlas/`** — the SwiftUI app, organized as:
  - `App/` — entry point, `@Observable` `AppModel`, root view, orientation gate
  - `Models/` — SwiftData `@Model` types (one per file: subscriptions, playlists, history)
  - `Features/` — feature-grouped screens (Feed, Channels, Search, Player, Downloads, Playlists, Profile, Recommendations)
  - `Components/` — reusable views (`VideoRow`, `Thumbnail`, `Avatar`, `CreatorChannelControl`, `GroupedVideoList`, …)
  - `Support/` — non-UI helpers (`Format`, `LoadPhase`, `ThumbnailPrefetcher`, `YouTubeCollaborators`)
- **`project.yml`** — source of truth for the Xcode project (generated by [xcodegen](https://github.com/yonaskolb/XcodeGen)).

### Why instance-switching is core
Piped is federated; every request hits an instance whose `api_url` you choose. Public instances are
frequently rate-limited or degraded (some return no `hls`/`audioStreams`), so the player falls back to
the best progressive stream and Settings lets you switch instances — including your own self-hosted one
(the recommended default for reliability).

## Build & run

```sh
# 1. (re)generate the Xcode project
xcodegen generate

# 2. open it
open Atlas.xcodeproj

# 3. select your iPhone, set the signing team if needed, ⌘R
```

Or from the CLI for the simulator:

```sh
xcodebuild -project Atlas.xcodeproj -scheme Atlas \
  -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO
```

Signing team and bundle id live in `project.yml` (`DEVELOPMENT_TEAM`, `PRODUCT_BUNDLE_IDENTIFIER`).

## Configuration

Atlas does not ship with a default Piped instance. Set one at runtime in
**Profile → Settings → Instance** before using online video features.

## Roadmap

- Audio-only mode
- Playback queue
- Related videos — in the Info sheet or a dedicated tab
- Accounts / Piped account sync
- Push notifications for new uploads

> **App Store note:** third-party YouTube frontends have historically faced App Review scrutiny
> (5.2.3 / content rights). Plan distribution accordingly.
